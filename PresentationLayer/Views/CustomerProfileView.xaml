<UserControl x:Class="PresentationLayer.Views.CustomerProfileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PresentationLayer.Views"
             xmlns:vm="clr-namespace:PresentationLayer.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.DataContext>
        <vm:CustomerProfileViewModel/>
    </UserControl.DataContext>
    
    <DockPanel Background="White">
        <!--  Dockpanel Top  -->
        <StackPanel DockPanel.Dock="Top" Orientation="Vertical">
            <Border Width="525" Height="50">
                <TextBlock Text="Visa kundprofil"
         FontSize="30"
         FontFamily="Roboto-Light"
         HorizontalAlignment="Left"
         Margin="15,15,10,0" Width="400"/>
            </Border>
            
            <StackPanel Orientation="Horizontal">
                <Label Content="Personnummer"/>
                <TextBox Text="{Binding SearchValue}" Width="70"/>
                <Button Content="Sök kund"/>
            </StackPanel>
            
        </StackPanel>

        <StackPanel DockPanel.Dock="Left" Orientation="Vertical">
            <StackPanel Orientation="Horizontal">
                <TextBox Text="Förnamn"/>
                <TextBox Text="Efternamn"/>
                <TextBox Text="Personnummer/SSN"/>
            </StackPanel>
            
            <StackPanel Orientation="Horizontal">
                <TextBox Text="Adress"/>
                <TextBox Text="Postnummer"/>
                <TextBox Text="Postadress"/>
            </StackPanel>
            
            <StackPanel Orientation="Horizontal">
                <TextBox Text="Email"/>
                <TextBox Text="Telefonnummer"/>
                <TextBox Text="Telefonnummer arbetsplats"/>
            </StackPanel>


            <!-- Notes DataGrid -->
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                
                <DataGrid Grid.Column="0" Height="100" Width="320" Margin="5,10,0,0" HorizontalAlignment="Left" VerticalAlignment="Top"                    
                    AutoGenerateColumns="False" 
                    CanUserAddRows="False"
                    BorderBrush="Black">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Datum" Width="60"/>
                        <DataGridTextColumn Header="Agentnummer" Width="90"/>
                        <DataGridTextColumn Header="Notering" Width="230"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
            <Label Content="Anteckning"/>
            <TextBox Text="{Binding Note}"/>
            <Button Content="Lägg till anteckning"/>




        </StackPanel>

    </DockPanel>
</UserControl>

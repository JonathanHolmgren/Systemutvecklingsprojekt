<UserControl x:Class="PresentationLayer.Views.ExportBillingInformationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PresentationLayer.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Background="White"
             xmlns:vm="clr-namespace:PresentationLayer.ViewModels">
    <UserControl.DataContext>
        <vm:ExportBillingInformationViewModel/>
    </UserControl.DataContext>
    <Grid>

        <ListBox Height="200"
         Width="300"
         Margin="-300,0,100,0"
         ItemsSource="{Binding PrivateCustomers}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <!-- Information om PrivateCustomer visas här -->
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding FirstName}" Margin="0,0"/>
                            <TextBlock Text="{Binding LastName}" Margin="3,0"/>
                            <TextBlock Text="{Binding SSN}" Margin="3,0"/>
                            <TextBlock Text="{Binding StreetAddress}" Margin="3,0"/>
                            <TextBlock Text="{Binding PostalCodeCity.City}" Margin="3,0"/>
                        </StackPanel>

                        <!-- Lista med försäkringar för varje kund -->
                        <ItemsControl ItemsSource="{Binding Insurances}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="Insurance: " FontWeight="Bold" Margin="0,0"/>
                                        <TextBlock Text="{}" Margin="5,0"/>
                                        <TextBlock Text="Premium: " FontWeight="Bold" Margin="10,0"/>
                                        <TextBlock Text="{}" Margin="5,0"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <ListBox Height="200"
                 Width="300"
                 Margin="450,0,100,0"
                 ItemsSource="{Binding CompanyCustomers}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                            <!-- Ändra till horisontell orientering -->
                                <TextBlock Text="{Binding CompanyName}" Margin="0,0"/>
                                <TextBlock Text="{Binding OrganisationNumber}" Margin="3,0"/>
                                <TextBlock Text="{Binding ContactPersonName}" Margin="3,0"/>
                                <TextBlock Text="{Binding StreetAddress}" Margin="3,0"/>
                                <TextBlock Text="{Binding PostalCodeCity.City}" Margin="3,0"/>

                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
        </ListBox>

        <Label Content="Privata faktura uppgifter" FontSize="15" HorizontalAlignment="Left" Margin="56,39,0,0" VerticalAlignment="Top"/>
        <Label Content="Företag faktura uppgifter" FontSize="15" HorizontalAlignment="Left" Margin="430,39,0,0" VerticalAlignment="Top"/>
        <Button Content="Button" HorizontalAlignment="Left" Margin="645,392,0,0" VerticalAlignment="Top" Height="36" Width="112"/>

    </Grid>
</UserControl>

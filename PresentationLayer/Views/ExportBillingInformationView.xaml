<UserControl x:Class="PresentationLayer.Views.ExportBillingInformationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PresentationLayer.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Background="White"
             xmlns:vm="clr-namespace:PresentationLayer.ViewModels">
    <UserControl.DataContext>
        <vm:ExportBillingInformationViewModel/>
    </UserControl.DataContext>

    <Grid Margin="20">
        <!-- Added Margin for the whole grid -->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Set to Auto to fit content -->
            <RowDefinition Height="*"/>
            <!-- Allow DataGrid to fill the remaining space -->
        </Grid.RowDefinitions>

        <StackPanel Orientation="Vertical" Margin="0,0,0,20" Grid.Row="0">
            <!-- Adjusted Margin -->
            <!-- Radio Buttons to switch between views -->
            <RadioButton Content="Privata Kunder" IsChecked="{Binding IsPrivateCustomerSelected}" GroupName="CustomerType" Margin="5"/>
            <RadioButton Content="Företags Kunder" IsChecked="{Binding IsCompanyCustomerSelected}" GroupName="CustomerType" Margin="5"/>
        </StackPanel>

        <!-- Content Control to switch between DataGrids -->
        <ContentControl Margin="0" Grid.Row="1">
            <!-- Adjusted Margin for ContentControl -->
            <ContentControl.Style>
                <Style TargetType="ContentControl">
                    <Setter Property="Content">
                        <Setter.Value>
                            <DataGrid ItemsSource="{Binding InsuranceInfoPrivateCustomer}" AutoGenerateColumns="False">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Namn" Binding="{Binding FullName}" />
                                    <DataGridTextColumn Header="Personnummer" Binding="{Binding SSN}" />
                                    <DataGridTextColumn Header="Address" Binding="{Binding Address}" />
                                    <DataGridTextColumn Header="Postnummer" Binding="{Binding PostalCode}" />
                                    <DataGridTextColumn Header="Ort" Binding="{Binding City}" />
                                    <DataGridTextColumn Header="Total Premium" Binding="{Binding TotalPremium}" />
                                    <DataGridTextColumn Header="Försäkringar" Binding="{Binding InsuranceSummary}" />
                                </DataGrid.Columns>
                            </DataGrid>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsCompanyCustomerSelected}" Value="True">
                            <Setter Property="Content">
                                <Setter.Value>
                                    <DataGrid ItemsSource="{Binding InsuranceInfoCompanyCustomer}" AutoGenerateColumns="False">
                                        <DataGrid.Columns>
                                            <DataGridTextColumn Header="Företagsnamn" Binding="{Binding CompanyName}" />
                                            <DataGridTextColumn Header="Organisationsnummer" Binding="{Binding OrganisationNumber}" />
                                            <DataGridTextColumn Header="Kontaktperson" Binding="{Binding ContactPerson}" />
                                            <DataGridTextColumn Header="Address" Binding="{Binding Address}" />
                                            <DataGridTextColumn Header="Postnummer" Binding="{Binding PostalCode}" />
                                            <DataGridTextColumn Header="Ort" Binding="{Binding City}" />
                                            <DataGridTextColumn Header="Total Premium" Binding="{Binding TotalPremium}" />
                                            <DataGridTextColumn Header="Försäkringar" Binding="{Binding InsuranceSummary}" />
                                        </DataGrid.Columns>
                                    </DataGrid>
                                </Setter.Value>
                            </Setter>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ContentControl.Style>
        </ContentControl>

    </Grid>
</UserControl>

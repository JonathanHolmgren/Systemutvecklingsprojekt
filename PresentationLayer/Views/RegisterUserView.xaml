<UserControl x:Class="PresentationLayer.Views.RegisterUserView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:PresentationLayer.ViewModels"
             xmlns:models="clr-namespace:Models;assembly=Models"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <UserControl.Resources>
        <!-- Provide AuthorizationLevel enum values -->
        <ObjectDataProvider MethodName="GetValues" ObjectType="{x:Type sys:Enum}" x:Key="AuthorizationLevelEnumValues">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="models:AuthorizationLevel" />
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
    </UserControl.Resources>

    <UserControl.DataContext>
        <vm:RegisterUserViewModel/>
    </UserControl.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Add User Section -->
        <GroupBox Header="Add User" Grid.Row="0" Margin="10">
            <StackPanel>

                <!-- Search for Employee -->
                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                    <Label Content="Find Employee:" Width="100"/>
                    <TextBox Name="employeeSearchTextBox" Width="200" Margin="0,0,10,0" />
                    <Button Content="Search" Width="75"/>
                </StackPanel>

                <!-- Generate Password -->
                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                    <Label Content="Generated Password:" Width="100"/>
                    <TextBox Name="passwordTextBox" 
                             Width="200" 
                             IsReadOnly="True" 
                             Margin="0,0,10,0" 
                             Text="{Binding PasswordInput, UpdateSourceTrigger=PropertyChanged}"/>
                    <Button Content="Generate" Width="75" Command="{Binding GeneratePasswordCommand}"/>
                </StackPanel>

                <!-- Role/Permission Level -->
                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                    <Label Content="Permission Level:" Width="100" />
                    <ComboBox 
                        Name="roleComboBox" 
                        Width="200" 
                        ItemsSource="{Binding Source={StaticResource AuthorizationLevelEnumValues}}" 
                        SelectedItem="{Binding AuthorizationLevelSelected, Mode=TwoWay}" />
                </StackPanel>

                <!-- Add User Button -->
                <Button Content="Add User" Width="100" HorizontalAlignment="Left" Command="{Binding AddUserCommand}"/>

                <!-- Display Password in Real Time -->
                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                    <Label Content="Password Display:" Width="100"/>
                    <TextBox Name="passwordDisplayTextBox" 
                             Width="200" 
                             Margin="0,0,10,0" 
                             Text="{Binding PasswordInput, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>
            </StackPanel>
        </GroupBox>

        <!-- Remove User Section -->
        <GroupBox Header="Remove User" Grid.Row="1" Margin="10">
            <StackPanel>
                <!-- Search for User -->
                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                    <Label Content="Find User:" Width="100"/>
                    <TextBox Name="userSearchTextBox" Width="200" Margin="0,0,10,0"/>
                    <Button Content="Search" Width="75"/>
                </StackPanel>

                <!-- Remove User Button -->
                <Button Content="Remove User" Width="100" HorizontalAlignment="Left"/>
            </StackPanel>
        </GroupBox>
    </Grid>
</UserControl>

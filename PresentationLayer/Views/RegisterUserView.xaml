<UserControl x:Class="PresentationLayer.Views.RegisterUserView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:PresentationLayer.ViewModels"
             xmlns:models="clr-namespace:Models;assembly=Models"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <UserControl.Resources>
        <!-- Provide AuthorizationLevel enum values -->
        <ObjectDataProvider MethodName="GetValues" ObjectType="{x:Type sys:Enum}" x:Key="AuthorizationLevelEnumValues">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="models:AuthorizationLevel" />
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
    </UserControl.Resources>

    <UserControl.DataContext>
        <vm:RegisterUserViewModel/>
    </UserControl.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Add User Section -->
        <GroupBox Header="Add User" Grid.Row="0" Margin="10">
            <StackPanel>

                <!-- Search for Employee -->
                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                    <Label Content="Find Employee:" Width="100"/>
                    <TextBox Name="employeeInput" Text="{Binding EmployeeInput, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="200" Margin="0,0,10,0" />
                    <Button Content="Search" Width="75" Command="{Binding SearchEmployee}"/>
                    <TextBlock Text="{Binding EmployeeSelected.FirstName}" Width="200" Margin="0,0,10,0" />
                </StackPanel>

                <!-- Password -->
                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                    <Label Content="Password:" Width="100"/>
                    <TextBox Name="passwordInput" 
                             Width="200" 
                             Margin="0,0,10,0" 
                             Text="{Binding PasswordInput, Mode=TwoWay}"/>
                    <!-- <Button Content="Generate" Width="75" Command="{Binding GeneratePasswordCommand}"/> -->
                </StackPanel>

                <!-- Role/Permission Level -->
                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                    <Label Content="Permission Level:" Width="100" />
                    <ComboBox 
                        Name="roleComboBox" 
                        Width="200" 
                        ItemsSource="{Binding Source={StaticResource AuthorizationLevelEnumValues}}" 
                        SelectedItem="{Binding AuthorizationLevelSelected, Mode=TwoWay}" />
                </StackPanel>

                <!-- Add User Button -->
                <Button Content="Add User" Width="100" HorizontalAlignment="Left" Command="{Binding AddUserCommand}"/>
            </StackPanel>
        </GroupBox>

        <!-- Remove User Section -->
        <GroupBox Header="Remove User" Grid.Row="1" Margin="10">
            <StackPanel>
                <!-- Search for User -->
                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                    <Label Content="Find User:" Width="100"/>
                    <TextBox Text="{Binding EmployeeInput, Mode=TwoWay}" Width="200" Margin="0,0,10,0"/>
                    <Button Command="{Binding SearchUser}" Content="Search" Width="75"/>
                </StackPanel>

                <ListBox ItemsSource="{Binding Users}" SelectedItem="{Binding UserSelected, Mode=TwoWay}" HorizontalAlignment="Left" VerticalAlignment="Top" Width="200" Height="150">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <!-- Visar UserID -->
                                <TextBlock Text="{Binding UserID}" Width="100"/>
                        
                                <!-- Visar Employee's AgentNumber -->
                                <TextBlock Text="{Binding AuthorizationLevel}" Width="150"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
                <!-- Remove User Button -->
                <Button Content="Remove User" Command="{Binding RemoveUser}" Width="100" HorizontalAlignment="Left"/>
            </StackPanel>
        </GroupBox>
    </Grid>
</UserControl>
